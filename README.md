# Widget Layout Experiment

This Flutter app implements the Student Profile / Dashboard UI for the experiment.

Run instructions:

```bash
flutter doctor
flutter devices
cd widget_layout
flutter pub get
flutter run
```
